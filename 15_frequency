elements = input("Enter elements of the list: ").split()

frequency = {}

for item in elements:
    if item in frequency:
        frequency[item] += 1
    else:
        frequency[item] = 1

print("\nFrequency of each element:")
for key, value in frequency.items():
    print(f"{key}: {value}")
